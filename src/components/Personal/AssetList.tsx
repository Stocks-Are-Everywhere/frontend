import { useEffect } from "react";
import { useNavigate } from "react-router-dom";

const AssetList : React.FC = () => {
    const navigate = useNavigate();

    return <h1>Asset List!!</h1>;
};

export default AssetList;